BASEDIR=/var/lib/${artifactId}
LOGDIR=/var/log/${artifactId}
HEAPSIZE=1024m
CONFFILE=/etc/clarind/${artifactId}.yaml
SERVICEJARFILE=/usr/share/${artifactId}/${artifactId}.jar
CONTEXTPATH=/${artifactId}
export LOGDIR CONTEXTPATH BASEDIR HEAPSIZE CONFFILE SERVICEJARFILE
